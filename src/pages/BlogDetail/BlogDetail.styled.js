import { Box, styled } from '@mui/material'

export const ctnComponent = styled(Box)(() => ({
    width: '90rem',
    display: 'flex',
    alignItems: 'flex-start',
    padding: '3rem 8.69rem 0 8.63rem',
    gap: '1rem',
    position: 'relative',
}))

export const container = styled(Box)(() => ({
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
}))
